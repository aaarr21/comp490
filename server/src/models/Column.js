class Column {
  constructor(title, columnAsg, color, creator) {
    this.title = title;
    this.columnAsg = columnAsg;
    this.color = color;
    this.creator = creator;
  }

  // Do later, generic asynchronous getters and setters
}

module.exports = Column;
